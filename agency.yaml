build:
  command: pip install -e .

start:
  command: uvicorn tipmaster_ai.main:app --host 0.0.0.0 --port 8000

ready:
  command: curl -f http://localhost:8000/health || exit 1

@app.get("/health")
async def health():
    return {"status": "ok"}
